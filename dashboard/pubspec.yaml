name: dashwing_dashboard
description: "Dashboard for dashwing"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.3

dependencies:
  async: ^2.13.0
  collection: ^1.19.1
  essential_flutter: ^1.1.0
  flutter:
    sdk: flutter
  http_toolkit: ^1.0.0+1
  shadcn_ui: ^0.40.5
  stream_transform: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono: ^1.35.0

flutter:
  uses-material-design: false
